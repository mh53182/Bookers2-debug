<div>
  <%= form_with model:[book, book_comment], local: false do |f| %>
    <%= f.text_area :comment, rows: '5', class: "w-100", id: "comment_textarea" %>
    <%= f.submit "送信" %>
  <% end %>
</div>

<!--非同期通信化のため、form_withには"local: false"を追記-->